#include <stdio.h>

/**
 * main - Entry of the program
 *
 * Return: Always 0 (on successful execution)
 */
int main(void)
{
	char name[] = "ALX student";

	printf("Name: %s\n", name);

	return (0);
}

